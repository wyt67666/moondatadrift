# Demo Report

Run:

```bash
moon run cmd/main
```

Expected shape of output:

- report title
- baseline and current row counts
- one section per shared column
- numeric columns show quantiles, PSI, KS-like score, and verdict
- categorical columns show top category shift and verdict
